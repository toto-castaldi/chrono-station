# Tag immagine da deployare. La CI lo imposta allo SHA del commit; default 'latest'.
TAG=latest

# Indirizzo del sito per Caddy.
#   :80                  -> solo HTTP
#   chrono.example.com   -> HTTPS automatico (certificati Let's Encrypt)
SITE_ADDRESS=:80
